# For hosted environments, set FRONTEND_URL to the actual domain of the frontend (e.g. https://my-frontend.vercel.app)
# You can specify multiple comma-separated URLs if needed.
FRONTEND_URL=http://localhost:3000
